#include <stdio.h>
#include <stdlib.h>

int main(){

    int num,*vetor, media = 0;

    scanf("%d",&num);

    vetor = (int *) malloc(num * sizeof(int));

    for(int i = 0; i < num; i++){
        scanf("%f",&vetor[i]);
    }

    for(int i = 0; i < num; i++){
        if 


    }
        
    


}